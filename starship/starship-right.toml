format = """
$status\
$cmd_duration\
$time"""

add_newline = false

[cmd_duration]
min_time = 500
format = "[took](bold) [$duration](bold yellow) [at](bold) "


[status]
#symbol = "✘"
format = '[$symbol$status $common_meaning ]($style) '
map_symbol = true
disabled = false

[time]
disabled = false
format = '🕙 [$time ]($style) '
time_format = "%r"
